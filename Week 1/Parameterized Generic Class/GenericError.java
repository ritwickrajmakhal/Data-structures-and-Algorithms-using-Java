/*
public class GenericError<T> {
    T array[];
    GenericError(T array[]){
        this.array = array;
    }
    double average(){
        double sum = 0.0;
        for (int i = 0; i < array.length; i++) {
            sum += array[i].doubleValue(); // ERROR
        }
        return sum/array.length;
    }
}
*/
